#include <iostream>
#include <string>

using namespace std;

int main() {
    string p,temph= "",tempm= "",temps= "";
    int h,m,s;
    cin >> p;
    for(int i=0;i<p.length();i++){
        if(i==0 || i==1){
            temph+= p[i];
            h= stoi(temph);
            cout << "\ntemp " << temph;
            cout <<"\nint" << h ;
        }
        
    }
    
do {
true
}while(true);
}