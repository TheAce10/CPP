#include <iostream>
#include <string>
#include <cstring>
#include <conio.h>

int main(){
    const char* str= "Cashmo Day";
    int len = strlen(str);

    char* ptr;
    ptr= new char[len+1];

    strcpy(ptr, str);
    // delete(l);



while(true){
    int new=1,old=0,temp;
    if (term<2){
    temp=new;
    new= new+old;
    old= temp;
    }
}
    return 0;
}